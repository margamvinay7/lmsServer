// import { Router } from 'express'
// import * as certController from '../controllers/certificate.controller'
// import { validate } from '../middleware/validate'
// import { CreateCertificationSchema, UpdateCertificationSchema } from '../validators/certificate.schema'
// import { authenticate } from '../middleware/auth'

// const router = Router()

// router.post('/', authenticate, validate(CreateCertificationSchema), certController.createCertification)
// router.get('/', authenticate, certController.getAllCertifications)
// router.get('/:id', authenticate, certController.getCertificationById)
// router.put('/:id', authenticate, validate(UpdateCertificationSchema), certController.updateCertification)
// router.delete('/:id', authenticate, certController.deleteCertification)

// export default router
